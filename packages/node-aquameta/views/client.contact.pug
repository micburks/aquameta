extends layout.pug
block content
    h1 title
    p body text
    form(method='POST' onSubmit='debugger; return (console.log(datum), debugger, false);')
        input(type='text' placeholder='placeholder') 
        input(type='submit' value='Send')

    script(src='/js/client.contact.js')
