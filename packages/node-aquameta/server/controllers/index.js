const contact = require('./contact');

// Bring in all controllers and attach them to a single controller object for
// simplicity in the router
module.exports = {
    contact /* shorthand for 'contact: contact' */
};
